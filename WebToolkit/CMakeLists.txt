file(GLOB_RECURSE SOURCES *.cpp *.h)
add_library(WebToolkit ${SOURCES})
if(UNIX)
	add_definitions(-D_FILE_OFFSET_BITS=64)
	target_link_libraries(WebToolkit pthread)
else(WIN32)
	target_link_libraries(WebToolkit ws2_32)
endif()
